program P
    const int size = 10, height = 12;
    int x, y, z;
    const string dz = "dzabe";
    const bool b = true;
    const char c = 'd';
    // const Tip t = 500;
    class Table 
    {
        int pos[], neg[];
        {
            void putp (int a, int idx) { this.pos[idx]=a; }
            void putn (int a, int idx) { this.neg[idx]=a; }
            int getp (int idx) { return pos[idx]; }
            int getn (int idx) { return neg[idx]; }
        }
    }
    Table val;
    const string str = "Ja sam string o da";
{
    void main() 
    int x, i;
    string sz;
    char c;
    {
        //‐‐‐‐‐‐‐‐‐‐ Initialize val
        val = new Table;
        val.pos = new int [size];
        val.neg = new int [size];
        i = 0;
        while (i < size) 
        {
            val.putp(0,i);
            val.putn(0,i);
            i++;
        }
        //‐‐‐‐‐‐‐‐‐‐ Read values
        read(x);
        while (x > 0) {
        if (0 <= x && x < size) 
        {
            val.putp(val.getp(x)+1, 1);
        } 
        else if ( -size < x && x < 0) 
        {
            val.putn(val.getn(-x)+1, 1);
        }
        read(x);
        }
    }
}